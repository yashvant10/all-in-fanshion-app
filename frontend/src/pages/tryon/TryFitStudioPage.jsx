import PageTemplate from '../../components/ui/PageTemplate';

export default function TryFitStudioPage() {
  return (
    <PageTemplate title="TryFit Studio" subtitle="Compose virtual looks with drag-and-drop layers.">
      <p className="text-sm text-slate-600">This module is wired for the AI Fashion SaaS workflow and ready for API integration.</p>
    </PageTemplate>
  );
}
